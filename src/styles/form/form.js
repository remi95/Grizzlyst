import colors from "../../constants/colors";

const form = {
    inputText: {
        width: 350,
        borderColor: colors.GRAY,
        borderBottomWidth: 1,
        margin: 10,
        padding: 10,
    },
    inputError: {
        borderColor: colors.RED,
    },
    textError:  {
        fontSize: 12,
        color: colors.RED,
        marginLeft: 20,
    },
    btnSubmit: {
        marginTop: 50,
        width: 250,
    },
};

export default form;